# ── Map ──────────────────────────────────────────────────────────────────────
# No map key needed. The app uses Leaflet + OpenStreetMap (100% free, no account).

# ── Backend (set when your partner's API is ready) ───────────────────────────
VITE_API_BASE_URL=

# ── WebSocket (set when backend has a live tracking feed) ────────────────────
VITE_WS_URL=
